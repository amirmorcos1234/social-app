import { horizontalScale } from '@utils/Scaling/scaling';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  avatar: {
    height: horizontalScale(40),
    width: horizontalScale(40),
  },
});
